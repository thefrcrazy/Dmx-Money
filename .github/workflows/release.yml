name: Release
on:
  push:
    tags:
      - 'v*'

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Create Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create ${{ github.ref_name }} \
            --title "DmxMoney ${{ github.ref_name }}" \
            --notes "Voir le [CHANGELOG](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) pour les détails." \
            --draft=false \
            --prerelease=false || echo "Release already exists"

  build-and-upload:
    needs: create-release
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target aarch64-apple-darwin'
            arch: 'aarch64'
            os_name: 'darwin'
          - platform: 'macos-latest'
            args: '--target x86_64-apple-darwin'
            arch: 'x64'
            os_name: 'darwin'
          - platform: 'windows-latest'
            args: ''
            arch: 'x64'
            os_name: 'windows'
          - platform: 'ubuntu-22.04'
            args: ''
            arch: 'x64'
            os_name: 'linux'

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libjavascriptcoregtk-4.1-dev

      - name: Install Frontend dependencies
        run: bun install

      - name: Switch to Modern Mode
        run: |
          chmod +x ./switch-tailwind.sh
          ./switch-tailwind.sh modern
        shell: bash

      # Build sans release automatique
      - name: Build Tauri App
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          includeUpdaterJson: false
          args: ${{ matrix.args }}

      # Gestion manuelle des assets et signature
      - name: Process and Upload Assets
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SIGNING_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          SIGNING_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          ARCH: ${{ matrix.arch }}
          OS_NAME: ${{ matrix.os_name }}
          VERSION: ${{ github.ref_name }} # e.g. v0.1.5
        run: |
          echo "🔍 Finding assets..."
          
          CLEAN_VERSION="${VERSION#v}"
          
          # Recherche plus large
          UPDATE_BUNDLE=$(find . -type f \( -name "*.app.tar.gz" -o -name "*.nsis.zip" -o -name "*.msi.zip" -o -name "*.AppImage.tar.gz" \) ! -path "*/deps/*" ! -path "*/.git/*" | head -n 1)
          
          if [ -z "$UPDATE_BUNDLE" ]; then
            echo "❌ No update bundle found!"
            ls -R src-tauri/target/release/bundle || ls -R src-tauri/target
            exit 1
          fi
          echo "📦 Found update bundle: $UPDATE_BUNDLE"
          
          # Extraction extension
          FILENAME=$(basename "$UPDATE_BUNDLE")
          if [[ "$FILENAME" == *.app.tar.gz ]]; then EXT="app.tar.gz"
          elif [[ "$FILENAME" == *.AppImage.tar.gz ]]; then EXT="AppImage.tar.gz"
          elif [[ "$FILENAME" == *.nsis.zip ]]; then EXT="nsis.zip"
          elif [[ "$FILENAME" == *.msi.zip ]]; then EXT="msi.zip"
          else EXT="${FILENAME##*.}"
          fi
          
          NEW_BUNDLE_NAME="DmxMoney_${CLEAN_VERSION}_${ARCH}_${OS_NAME}.${EXT}"
          
          echo "🔄 Renaming bundle to $NEW_BUNDLE_NAME"
          cp "$UPDATE_BUNDLE" "./$NEW_BUNDLE_NAME"
          
          # Signature
          echo "✍️ Signing $NEW_BUNDLE_NAME..."
          export TAURI_SIGNING_PRIVATE_KEY="$SIGNING_KEY"
          export TAURI_SIGNING_PRIVATE_KEY_PASSWORD="$SIGNING_KEY_PASSWORD"
          bun tauri signer sign "./$NEW_BUNDLE_NAME"
          
          SIG_FILE="./$NEW_BUNDLE_NAME.sig"
          if [ ! -f "$SIG_FILE" ]; then
             echo "❌ Signature creation failed"
             exit 1
          fi
          
          # Upload Bundle + Signature
          echo "🚀 Uploading bundle and signature..."
          gh release upload "$VERSION" "./$NEW_BUNDLE_NAME" "$SIG_FILE" --clobber
          
          # Upload Installeurs renommés
          echo "🔍 Searching for installers..."
          find . -type f \( -name "*.dmg" -o -name "*.exe" -o -name "*.msi" -o -name "*.AppImage" -o -name "*.deb" \) ! -path "*/deps/*" ! -path "*/.git/*" | while read -r INSTALLER; do
            INST_NAME=$(basename "$INSTALLER")
            # Nom unique: DmxMoney_0.1.6_aarch64_darwin.dmg
            NEW_INST_NAME="${INST_NAME%.*}_${ARCH}_${OS_NAME}.${INST_NAME##*.}"
            cp "$INSTALLER" "./$NEW_INST_NAME"
            echo "🚀 Uploading installer: $NEW_INST_NAME"
            gh release upload "$VERSION" "./$NEW_INST_NAME" --clobber
          done

  generate-updater-json:
    needs: build-and-upload
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Generate multi-platform latest.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ github.ref_name }}"
          CLEAN_VERSION="${VERSION#v}"
          PUB_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          REPO="${{ github.repository }}"
          BASE_URL="https://github.com/$REPO/releases/download/$VERSION"

          echo "📦 Fetching release assets for $VERSION..."
          ASSETS=$(gh release view "$VERSION" --repo "$REPO" --json assets -q '.assets[].name')

          PLATFORMS="{}"

          add_platform() {
            local platform_key="$1"
            local pattern="$2"

            # On cherche le fichier .sig correspondant au pattern
            # Le pattern doit matcher le nom du bundle (PAS la signature)
            ASSET_NAME=$(echo "$ASSETS" | grep -E "$pattern" | grep -v '\.sig$' | head -n 1)
            
            if [ -n "$ASSET_NAME" ]; then
              SIG_NAME="${ASSET_NAME}.sig"
              if echo "$ASSETS" | grep -qF "$SIG_NAME"; then
                echo "✅ $platform_key : $ASSET_NAME"
                # Download signature content
                gh release download "$VERSION" --repo "$REPO" -p "$SIG_NAME" -D /tmp --clobber
                SIGNATURE=$(cat "/tmp/$SIG_NAME")
                
                PLATFORMS=$(echo "$PLATFORMS" | jq \
                  --arg key "$platform_key" \
                  --arg sig "$SIGNATURE" \
                  --arg url "$BASE_URL/$ASSET_NAME" \
                  '. + {($key): {"signature": $sig, "url": $url}}')
              else
                echo "⚠️ Missing signature for $ASSET_NAME"
              fi
            else
              echo "ℹ️ No asset match for $platform_key with pattern $pattern"
            fi
          }

          # Patterns basés sur le renommage effectué dans build-and-upload
          # Nom format: DmxMoney_0.1.5_aarch64_darwin.app.tar.gz
          add_platform "darwin-aarch64" "DmxMoney_.*_aarch64_darwin\.app\.tar\.gz"
          add_platform "darwin-x86_64"  "DmxMoney_.*_x64_darwin\.app\.tar\.gz"
          add_platform "windows-x86_64" "DmxMoney_.*_x64_windows\.(msi|nsis)\.zip"
          add_platform "linux-x86_64"   "DmxMoney_.*_x64_linux\.AppImage\.tar\.gz"

          jq -n \
            --arg version "$CLEAN_VERSION" \
            --arg notes "Mise à jour vers la version $CLEAN_VERSION" \
            --arg pub_date "$PUB_DATE" \
            --argjson platforms "$PLATFORMS" \
            '{version: $version, notes: $notes, pub_date: $pub_date, platforms: $platforms}' > latest.json

          echo "📄 latest.json content:"
          cat latest.json

          echo "🚀 Uploading latest.json..."
          gh release upload "$VERSION" latest.json --clobber
