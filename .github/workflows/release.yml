name: Release
on:
  push:
    tags:
      - 'v*'

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest' # Apple Silicon (M1/M2/M3)
            args: '--target aarch64-apple-darwin'
          - platform: 'macos-latest' # Intel Mac (Cross-compiled from ARM runner)
            args: '--target x86_64-apple-darwin'
          - platform: 'windows-latest' # Windows x64
            args: ''
          - platform: 'ubuntu-22.04'   # Linux x64
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Install Linux dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libjavascriptcoregtk-4.1-dev

      - name: Install Frontend dependencies
        run: bun install

      # Force Modern Mode
      - name: Switch to Modern Mode
        run: |
          chmod +x ./switch-tailwind.sh
          ./switch-tailwind.sh modern
        shell: bash

      - name: Build and Publish
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'DmxMoney ${{ github.ref_name }}'
          includeUpdaterJson: true
          releaseBody: |
            ### 🚀 Nouvelles Fonctionnalités
            - **Sidebar Rétractable** : Gagnez de l'espace de travail.
            - **Édition Inline** : Modifiez vos transactions directement dans le tableau.
            - **Multi-sélection** : Actions groupées pour supprimer ou pointer.
            - **Mises à jour Automatiques** : Système de détection intégré.
            
            ### 🎨 Améliorations UI/UX
            - **Graphiques Premium** : Courbes lisses et réactives.
            - **Animations** : Interface fluide et rapide.
            - **Thème** : Mode sombre #121212 harmonisé.
            
            ### 🛠️ Technique
            - Tauri v2.1.1 (Stable).
            - Support macOS Intel & Apple Silicon.
            - Support Windows x64.
            - Support Linux.
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}